/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['lh3.googleusercontent.com', 'www.ionos.es', 's3.dualstack.us-east-2.amazonaws.com'], // Agrega el dominio aquí
    }
};

export default nextConfig;
